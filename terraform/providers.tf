# The kind provider is configured in main.tf. This file documents the
# local-only nature of this module: it provisions a cluster on the VPS,
# not in a cloud account. Swap for the AWS/EKS provider for a real deploy.
